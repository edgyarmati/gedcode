# Implementation Plans

Generated by the improve skill on 2026-06-13, audited against commit `65e913c7`
(branch `cleanup/drop-upstream-features`). Each executor: read the plan fully
before starting, run its drift check, honor its STOP conditions, and update your
row below when done.

These plans were produced by an 8-way parallel audit (all 9 categories), then
every finding turned into a plan was re-verified against the live code by hand.
Each plan is self-contained — an executor needs only the plan file and the repo.

**Verification gates used by every plan**: `bun fmt` · `bun lint` ·
`bun typecheck` · `bun run test` (never `bun test`).

**Session status (2026-06-13):** All 13 issue-backed plans are landed as PRs —
**#21 merged** (001); **#22–#31 open and CI-green** (Verify=pass on all). The
4 PRs that first failed CI at `fmt:check` (#23/#24/#26/#27) were fixed with
format-only `style:` commits and now pass. Only the 3 security plans
(008/009/010) are not started — held as local-only pending a decision on whether
to open them as PRs on this public fork. Merging the open PRs is the maintainer's
call.

**Review round (2026-06-14):** An external review pass flagged three issues,
all confirmed real against the live code and fixed on their PR branches:

- **#23** (blocking) — stopping a session with a pending `AskUserQuestion`
  double-emitted `user-input.resolved` and returned `behavior:"allow"` with
  empty answers (auto-approving the tool call). Fixed by routing stop through
  the same `cancel` path as an abort (single resolved event + `deny` result);
  regression test strengthened to assert the exact result. Commits
  `b4c488a6` + `3b16eefe`.
- **#26** (blocking) — the incremental activity slice fast-path lacked the
  message-path cap guard, so at `MAX_THREAD_ACTIVITIES` a tail-append diverged
  from the capped canonical array and left a stale `byId` entry. Fixed by
  mirroring the message-path guard; characterization test added. Commit
  `c3730a2f`.
- **#25** (non-blocking) — removed the dead turbo `test:coverage` task (the
  root script uses `turbo run test -- --coverage`). Commit `365d760c`.

All three re-verified (`fmt`/`lint`/`typecheck`/`test`) and pushed; review
comments answered on each PR.

**Merge + local build (2026-06-14):** Per operator instruction, all 10 open
PRs (#22–#31) were squash-merged into `cleanup/drop-upstream-features` (the
only CHANGELOG overlap, #23 vs #31, auto-resolved cleanly). The full merged
tree was then built and verified locally on macOS arm64 (mise toolchain:
node 24.13.1 / bun 1.3.11): `bun run build` ✓, `bun typecheck` ✓ (13/13),
`bun run test` ✓ (1020 passed, 1 skipped), `bun lint` ✓ (warnings only),
`bun run fmt:check` clean for all tracked files (only untracked `plans/*.md`
flagged). `bun run build:desktop` ✓. One pre-existing build break surfaced and
was fixed: the desktop `dev`/`start`/`smoke-test` turbo tasks referenced the
pre-rebrand `t3#build` package (renamed to `gedcode`); repaired to
`gedcode#build` (commit `0461dd8a`), after which `bun run test:desktop-smoke`
passes ("Desktop smoke test passed"). All of #22–#31 are now **MERGED**; the
3 security plans (008/009/010) remain local-only/unpublished.

## Execution order & status

| Plan | Title                                                           | Priority | Effort | Depends on                                                    | Issue                                                  | Status                                                                                                                |
| ---- | --------------------------------------------------------------- | -------- | ------ | ------------------------------------------------------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| 001  | Source-control docs list only shipped providers (GitHub+GitLab) | P1       | S      | —                                                             | [#8](https://github.com/edgyarmati/gedcode/issues/8)   | MERGED ([PR #21](https://github.com/edgyarmati/gedcode/pull/21))                                                      |
| 002  | TODO.md reflects only genuinely-unbuilt work                    | P2       | S      | —                                                             | [#9](https://github.com/edgyarmati/gedcode/issues/9)   | [PR #22](https://github.com/edgyarmati/gedcode/pull/22) open                                                          |
| 003  | Remove phantom turbo build tasks from source-only packages      | P2       | S      | —                                                             | [#10](https://github.com/edgyarmati/gedcode/issues/10) | [PR #25](https://github.com/edgyarmati/gedcode/pull/25) open                                                          |
| 004  | Add opt-in vitest coverage provider                             | P2       | S      | —                                                             | [#11](https://github.com/edgyarmati/gedcode/issues/11) | [PR #25](https://github.com/edgyarmati/gedcode/pull/25) open                                                          |
| 005  | Resolve pending Claude user-input requests on session stop      | P2       | S      | —                                                             | [#12](https://github.com/edgyarmati/gedcode/issues/12) | [PR #23](https://github.com/edgyarmati/gedcode/pull/23) open                                                          |
| 006  | Settle running turn on interrupt events without a turnId        | P2       | S      | —                                                             | [#13](https://github.com/edgyarmati/gedcode/issues/13) | [PR #26](https://github.com/edgyarmati/gedcode/pull/26) open                                                          |
| 007  | Surface swallowed WebSocket subscription errors                 | P2       | S      | —                                                             | [#14](https://github.com/edgyarmati/gedcode/issues/14) | [PR #24](https://github.com/edgyarmati/gedcode/pull/24) open                                                          |
| 008  | 🔒 Session cookie Secure + plaintext-HTTP auth gate             | P2       | S      | —                                                             | _(unpublished — security)_                             | TODO                                                                                                                  |
| 009  | 🔒 SSH host-spec argument-injection guard                       | P2       | S      | —                                                             | _(unpublished — security)_                             | TODO                                                                                                                  |
| 010  | 🔒 WS Origin allowlist + CORS scoping                           | P2       | S–M    | —                                                             | _(unpublished — security)_                             | TODO                                                                                                                  |
| 011  | Incremental thread-shell summary (drop O(n²) reload)            | P1       | M      | 004 (rec.)                                                    | [#15](https://github.com/edgyarmati/gedcode/issues/15) | [PR #27](https://github.com/edgyarmati/gedcode/pull/27) open                                                          |
| 012  | Incremental web store message/activity slices                   | P1       | M      | 004 (rec.)                                                    | [#16](https://github.com/edgyarmati/gedcode/issues/16) | [PR #26](https://github.com/edgyarmati/gedcode/pull/26) open                                                          |
| 013  | Shell stream payload without per-event per-subscriber re-query  | P1       | M      | 011 ([PR #27](https://github.com/edgyarmati/gedcode/pull/27)) | [#17](https://github.com/edgyarmati/gedcode/issues/17) | [PR #31](https://github.com/edgyarmati/gedcode/pull/31) open (approach B: shared hub in singleton engine)             |
| 014  | [SPIKE] Ged role-invocation: finish or remove                   | P2       | L      | —                                                             | [#18](https://github.com/edgyarmati/gedcode/issues/18) | [PR #29](https://github.com/edgyarmati/gedcode/pull/29) open (doc → recommends REMOVE dead path)                      |
| 015  | [SPIKE] Message queueing design                                 | P2       | L      | 002 (companion)                                               | [#19](https://github.com/edgyarmati/gedcode/issues/19) | [PR #30](https://github.com/edgyarmati/gedcode/pull/30) open (doc → recommends durable orchestration queue)           |
| 016  | [SPIKE] GUI remote-project add scoping                          | P3       | M      | —                                                             | [#20](https://github.com/edgyarmati/gedcode/issues/20) | [PR #28](https://github.com/edgyarmati/gedcode/pull/28) open (doc → concludes already works; REMOTE.md note is stale) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED
(one-line rationale).

## Orchestrator mode (separate initiative, started 2026-06-14)

Plans 017+ are **not** part of the 001–016 audit above — they are the first
phase of a new feature, **Orchestrator mode** (a per-project PM agent on
[`pi`](https://github.com/earendil-works/pi) orchestrating the existing
Codex/Claude/OpenCode workers). Design doc: `orchestrator-mode-design.md`.
Tracking epic: [#32](https://github.com/edgyarmati/gedcode/issues/32). Built on
branch `feat/orchestrator-mode`.

| Plan | Title                                                                                          | Priority | Effort | Depends on      | Issue                                                  | Status                                           |
| ---- | ---------------------------------------------------------------------------------------------- | -------- | ------ | --------------- | ------------------------------------------------------ | ------------------------------------------------ |
| 017  | [SPIKE] Verify `pi-agent-core`/`pi-ai` API + pin dependency                                    | P1       | M      | —               | [#32](https://github.com/edgyarmati/gedcode/issues/32) | DONE — verdict **GO (with changes)**             |
| 018  | Orchestrator mode — thin vertical slice (PM on pi, detached Codex handoff, plan gate, durable) | P1       | XL     | 017 (hard gate) | [#32](https://github.com/edgyarmati/gedcode/issues/32) | DONE — WP-A–N complete; full gates green locally |

018 decomposes into dependency-ordered work packages WP-A…WP-N; see the plan
file. It must not start until 017's decision doc
(`docs/decisions/2026-06-pi-agent-core-api.md`) recommends GO / GO-WITH-CHANGES.

🔒 = security-hardening plan. Per the operator's choice ("hold security, publish
the rest"), plans 008–010 are kept as local files only and were **not** published
as GitHub issues (the fork `edgyarmati/gedcode` is public; security details are
not disclosed there). The other 13 plans are published — issue URLs filled in
above and in each plan's Status block once created.

## Recommended order

1. **Quick wins first** (independent, low risk): 001, 002, 003, 004. These are
   small, mostly docs/config, and 004 unlocks coverage measurement for the perf
   work.
2. **Correctness bugs** (independent, small): 005, 006, 007.
3. **Security** (consider shipping 008 + 010 together): 008, 009, 010.
4. **Performance** (highest impact; land 011 before 013): 011, then 012 and 013.
5. **Direction spikes** (decisions for the maintainer): 014, 015, 016 — each
   produces a decision doc, not a finished feature.

## Dependency notes

- **004 → 011, 012** (recommended, not blocking): run `bun run test:coverage` on
  `ProjectionPipeline.ts` / `store.ts` first so the executor knows which branches
  are protected before refactoring.
- **011 → 013** (recommended): 011 makes the projected thread row the cheap
  single source that 013's shell-stream derivation reads from. Landing 011 first
  makes 013 simpler and lower-risk.
- **008 + 010** are companions (both harden the network-exposed surface);
  consider one combined review.
- **002 ↔ 015**: 002 leaves "Queueing messages" as the sole open TODO; 015 is its
  design spike. 015's build follow-up should remove that TODO line.
- All other plans are independent and can run in parallel.

## Findings considered and rejected (so they are not re-audited)

- **`apps/marketing/` and top-level `src/` "leftovers"** — untracked local build
  artifacts (`git ls-files` returns 0 for both). The upstream-removal commits
  were clean; nothing to do.
- **Cursor / PostHog / Bitbucket / Azure DevOps code removal incomplete** —
  rejected. Grep found zero dangling _source_ references; the removals were
  complete. Only the _docs_ lagged (that is plan 001). Remaining `cursor` hits
  are pagination/text cursors and the IDE "open in editor" target; `azure` hits
  are Azure Trusted Signing (desktop code-signing) — all intentional.
- **DIR-06 "scroll-to-bottom on submit"** — rejected as a build item: already
  implemented (`ChatView.tsx:2814` and `:3212` call `scrollToEnd` before adding
  the optimistic message). The auditor grepped the wrong path. Folded into plan
  002 (TODO.md reconcile) instead.
- **Pairing-token entropy** (~60 bits, `crypto.getRandomValues`) — adequate; not
  a finding.
- **git / source-control CLI argument construction** — safe (arg arrays + `--`
  separators, no shell); not a finding. (The SSH host-spec path is different and
  IS addressed — plan 009.)

## Findings surfaced but NOT planned this round

The audit produced ~30 vetted findings; the operator selected the bundles above
(13 fixes + 3 spikes). The following vetted findings were surfaced but not
selected for plans this round — they remain valid future work:

- **Bound the runtime/domain event PubSubs** (`OrchestrationEngine.ts:91`,
  `ProviderService.ts:211`) — unbounded PubSub/queues can grow memory under
  chatty turns; M effort, MED confidence.
- **Streaming-delta idempotency** (`store.ts:1434`) — in-session redelivery could
  double message text; reconnect is mitigated by snapshot.
- **Document the toolchain beta pinning + the Effect patch** (`package.json:6,16`,
  `patches/effect@4.0.0-beta.73.patch`) — HIGH confidence, mostly docs.
- **CI caching/parallelism** (`ci.yml`, `turbo.json` `cache:false`) — re-runs
  everything per push.
- **`.env.example`** missing despite ~50 env vars incl. `T3CODE_AUTH_TOKEN`.
- **Record the `.logic.ts`/`.browser.tsx` test-split convention** in AGENTS.md.
- **Split the ChatView god component** (`ChatView.tsx`, 3869 LOC) — L effort.
- **Document filesystem-browse trust boundary** (`WorkspaceEntries.ts:152`) —
  security; relates to plan 016.
- **Timing-safe pairing-credential compare** (`BootstrapCredentialService.ts:185`)
  — security hardening; a helper already exists.
- Lower-confidence: projector replay idempotency, detail/shell stream clobber,
  optimistic-cleanup re-runs, contracts schema-only borderline, editor-vs-tsgo TS
  SDK divergence, composer-draft per-keystroke serialize.

Re-run `/improve` (or `/improve reconcile`) to turn any of these into plans.
