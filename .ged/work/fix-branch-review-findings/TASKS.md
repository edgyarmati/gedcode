# TASKS: Fix branch review findings

1. Convert `packages/ged-workflow` tests/imports so TSGo typecheck passes.
2. Update desktop canonical identity values and launcher names/IDs to GedCode.
3. Preserve/update desktop legacy user-data fallback tests.
4. Update marketing repo constants, links, cache key, and demo text.
5. Implement sticky model selection clear semantics.
6. Add composer draft store tests for null and undefined sticky clears.
7. Run focused tests and required full checks.
8. Explicitly verify desktop fallback candidate list/precedence and package/build identity files.
9. Add or update tests for canonical/no-legacy path, legacy candidates, coexistence precedence, and relevant build identity metadata.
10. Run grep checks for unintended desktop/marketing `pingdotgg/t3code`, `t3code`, and T3 identity leftovers, documenting allowed compatibility exceptions.
