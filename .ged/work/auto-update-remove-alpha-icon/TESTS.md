# TESTS

Required:

- `bun fmt`
- `bun lint`
- `bun typecheck`
- `bun run test`

Focused:

- Branding/version tests in desktop, web, and release scripts.
- Update logic/UI tests for desktop update state/actions.
- Asset dimension checks for generated PNG/favicon/icon outputs.
- Grep audit for user-facing `Alpha`/`GedCode (Alpha)` references.
