# TASKS

1. Confirm current fork status and remotes.
2. Add visible upstream credit to public docs.
3. Verify formatting/checks for docs-only changes.
4. Detach the repo in GitHub Settings UI using the fork-network detach/convert-to-standalone action.
5. Verify `gh api repos/edgyarmati/gedcode --jq .fork` reports `false` after detach.
6. Confirm GitHub Sponsorship UI/button is available after detach.
