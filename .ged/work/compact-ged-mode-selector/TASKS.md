# Tasks

## Implementation

1. Update `ChatComposer.tsx` workflow trigger copy so selected labels are compact.
2. Preserve full dropdown labels/descriptions and accessible thread-mode labeling.
3. Remove the inline green Ged helper span beside `ProviderModelPicker`.
4. Align `CompactComposerControlsMenu.tsx` radio labels with compact copy.
5. Update focused browser test expectations for compact menu labels.

## Verification

1. Confirm Ged mode help remains available via title/dropdown text.
2. Confirm no server/contracts/settings/model-selection files changed.
3. Run required checks.
