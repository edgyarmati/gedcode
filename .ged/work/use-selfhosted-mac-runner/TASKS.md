# TASKS

1. Verify runner labels/status.
2. Update only the `macOS x64` release matrix runner.
3. Run focused workflow diff/format checks.
4. Commit and push.
5. Cancel stale release run and retag `v0.1.0` to the new commit.
6. Confirm a new release run starts and macOS x64 can be picked up by the self-hosted runner.
